import AdminSectionPage from "../components/layout/AdminSectionPage";

export default function RecordingsPage() {
  return (
    <AdminSectionPage
      eyebrow="Admin / Recordings"
      title="Recordings"
      description="Review captured meetings, playback history, storage usage, and retention status."
    />
  );
}
