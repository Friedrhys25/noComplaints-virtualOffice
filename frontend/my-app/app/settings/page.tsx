import AdminSectionPage from "../components/layout/AdminSectionPage";

export default function SettingsPage() {
  return (
    <AdminSectionPage
      eyebrow="Admin / Settings"
      title="Settings"
      description="Configure workspace defaults, permissions, notifications, and system preferences."
    />
  );
}
